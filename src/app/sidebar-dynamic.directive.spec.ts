import { SidebarDynamicDirective } from './sidebar-dynamic.directive';

describe('SidebarDynamicDirective', () => {
  it('should create an instance', () => {
    const directive = new SidebarDynamicDirective();
    expect(directive).toBeTruthy();
  });
});
